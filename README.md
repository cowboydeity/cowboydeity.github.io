<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Alexandra Ash | Academic Profile</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --maroon: #900008;
      --black: #1b1b1b;
      --gray: #f8f8f8;
      --white: #fff;
      --shadow: rgba(80,0,0,0.06);
    }
    html, body {
      margin: 0; padding: 0;
      background: var(--white);
      font-family: 'Libre Baskerville', Baskerville, serif;
      color: var(--black);
      min-height: 100vh;
      box-sizing: border-box;
      width: 100vw;
      overflow-x: hidden;
    }
    .container {
      max-width: 820px;
      margin: 0 auto;
      background: var(--gray);
      border-left: 3.5px solid var(--maroon);
      border-right: 3.5px solid var(--maroon);
      min-height: 100vh;
      box-shadow: 0 4px 24px var(--shadow);
      padding: 0 0 3rem 0;
    }
    .header {
      background: var(--white);
      border-bottom: 2px solid var(--maroon);
      padding: 3rem 2rem 1.4rem 2rem;
      text-align: center;
      position: relative;
      letter-spacing: .04em;
    }
    .profile-pic-container {
      margin-bottom: 1.4rem;
    }
    .profile-pic {
      width: 152px;
      height: 152px;
      object-fit: cover;
      border-radius: 50%;
      border: 4px solid var(--maroon);
      box-shadow: 0 4px 32px rgba(144,0,8,0.08);
      background: var(--gray);
      display: block;
      margin-left: auto;
      margin-right: auto;
    }
    .header .name {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 0.7rem;
    }
    .header .contact {
      font-size: 1.08rem;
      margin-bottom: 0.2rem;
      opacity: 0.8;
      font-family: 'Libre Baskerville', Baskerville, serif;
    }
    .header .profile-desc {
      font-size: 1.07rem;
      opacity: .75;
      margin-top: 0.7rem;
      letter-spacing: .02em;
    }
    section {
      margin: 0;
      padding: 2.2rem 2rem 0.8rem 2rem;
      border-bottom: 1px solid #ececec;
    }
    section:last-child {
      border-bottom: none; 
      padding-bottom: 2rem;
    }
    h2 {
      font-size: 1.25rem;
      color: var(--maroon);
      font-weight: bold;
      margin: 0 0 1.15rem 0;
      padding-bottom: .3rem;
      border-bottom: 1.7px solid var(--maroon);
      letter-spacing: 0.7px;
      font-family: 'Libre Baskerville', Baskerville, serif;
    }
    .institution, .role {
      font-weight: bold;
      font-size: 1.09rem;
      margin-top: 1rem;
      margin-bottom: 0;
      color: var(--black);
    }
    .subtitle {
      font-size: 1.015rem;
      font-style: italic;
      color: #3b3b3b;
      margin-bottom: .95rem;
      font-weight: 400;
      margin-top: .1rem;
    }
    .years {
      color: #555;
      font-size: 0.93em;
      opacity: 0.7;
    }
    .muted {
      color: #800009;
      font-size: 0.94rem;
      opacity: 0.62;
    }
    .bold {font-weight: bold;}
    ul, li {
      margin-left: 0;
      padding-left: 0;
      list-style: none;
    }
    ul {margin-bottom: 1.1rem;}
    li {margin-bottom: 0.65em; line-height: 1.5;}
    .button {
      display: inline-block;
      margin-top: 1.5rem;
      background: var(--maroon);
      color: var(--white);
      padding: 0.6em 2em;
      border-radius: 24px;
      font-size: 1rem;
      letter-spacing: 1px
